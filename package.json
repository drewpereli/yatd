{
  "name": "yatd",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"npm run dev -w frontend\" \"npm run dev -w backend\"",
    "prisma-gen": "npm run prisma-gen -w backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/frontend",
    "packages/backend"
  ],
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
